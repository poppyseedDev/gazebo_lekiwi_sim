# Upstream Audit — 2026-05-16 (2 weeks after Phase 1 baseline 91ed5c0)

## Summary

No action needed — all three upstream packages are unchanged since the 2026-05-02 baseline.

---

## m-explore-ros2

Source: `https://github.com/robo-friends/m-explore-ros2`

No commits have landed since 2026-05-02. The three most recent commits as of today are all pre-baseline:

- `86742bf` 2026-04-08 — "Update multi robot map merge launchs for humble distro" (charlielito) — Humble-only launch tweak, no Jazzy/explore-node impact.
- `d9969be` 2026-04-08 — "Update base image devcontainer" (charlielito) — CI/dev infrastructure only.
- `d18e5cd` 2026-03-20 — "Fix the marker positions (#48)" (Timple) — RViz marker viz fix, no param or algorithm change.

**Param-API impact on this repo:** None. The parameters used in
`src/lekiwi_gazebo/config/explore_lite.yaml` (`robot_base_frame`,
`costmap_topic`, `costmap_updates_topic`, `planner_frequency`,
`progress_timeout`, `min_frontier_size`, `potential_scale`, `gain_scale`,
`transform_tolerance`) are unchanged in the upstream explore node.

---

## slam_toolbox (Jazzy)

Source: `https://index.ros.org/p/slam_toolbox/#jazzy`

- **Baseline version (~2026-05-02):** 2.8.5 (ros.org last indexed 2026-04-29 — already the version in use at baseline)
- **Latest version:** 2.8.5
- **Breaking changes affecting our config:** None. No new release since the baseline.

> `apt-cache policy ros-jazzy-slam-toolbox` was unavailable in this remote env; version confirmed via ros.org index.

---

## nav2 (Jazzy)

Source: `https://index.ros.org/p/nav2_bringup/#jazzy` (and nav2_mppi_controller, nav2_amcl)

- **Baseline version (~2026-05-02):** 1.3.12 (all nav2 packages; ros.org last indexed 2026-04-29)
- **Latest version:** 1.3.12
- **Breaking changes affecting MPPI / AMCL / costmap / BT navigator:** None since baseline.

### Iron→Jazzy migration notes (pre-baseline, already handled)

For reference, the Iron→Jazzy migration that was current at the Phase 1 baseline included the
following changes in the areas this repo specifically tunes. All are already addressed:

| Area | Change | Repo status |
|---|---|---|
| BT navigator | `error_code_name_prefixes` (replaces `error_code_names`) | ✅ Already in `nav2_params.yaml:74` |
| cmd_vel format | `enable_stamped_cmd_vel` (defaults `false` = legacy `Twist`) | ✅ Repo uses `Twist`; no explicit flag needed |
| Collision Monitor | Polygon spec changed to `"[[p1.x,p1.y], ...]"` string format | ✅ Repo uses `FootprintApproach` (polygon-free approach type), unaffected |
| MPPI critics | `GoalCritic`, `PathAlignCritic`, `CostCritic` parameter tuning changes | ✅ Current critic list + weights compile cleanly against 1.3.12 |
| velocity_smoother | `vy` bounds default was 0 (diff-drive); must be set for holonomic | ✅ Repo explicitly sets `max_velocity: [0.4, 0.4, 1.5]` |
| AMCL | `set_initial_pose` / `initial_pose` block for auto-init | ✅ Already in `nav2_params.yaml:44–49` |

### Jazzy→Kilted migration preview (not yet applicable)

The upcoming Kilted release will default `cmd_vel` to `TwistStamped` (breaking). The fix when upgrading will be to add `enable_stamped_cmd_vel: false` to controller_server, velocity_smoother, and collision_monitor sections in `nav2_params.yaml`. No action now.

---

## Recommended actions

None — current config still works. All three upstream packages are at the same version as the
Phase 1 baseline (2026-05-02). No parameter renames, algorithm changes, or breaking API
changes have landed since `91ed5c0`.

When upgrading to Kilted (future), remember to pin `enable_stamped_cmd_vel: false` or migrate
cmd_vel topics to `TwistStamped`.
